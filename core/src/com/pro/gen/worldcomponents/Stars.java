package com.pro.gen.worldcomponents;

/**
 * Created by Gallo on 8/15/2015.
 */
public class Stars {
/*
    private MapItems starCircle = new MapItems(Item.CIRCLE_SMALL, 10, 3);
    private MapItems starTwinkle = new MapItems(Item.TWINKLE, 16, 9);
    private int amount;

    public Stars(int amount){
        this.amount = amount;
    }

    public MapItems[] getCircleStars(){
        MapItems[] stars = new MapItems[amount];
        for(int i = 0; i < amount; i++ ){
            stars[i] = starCircle;
        }
        return stars;
    }

    public MapItems[] getTwinkleStars(){
        MapItems[] stars = new MapItems[amount];
        for(int i = 0; i < amount; i++ ){
            stars[i] = starTwinkle;
        }
        return stars;
    }

    public MapItems[] getMixedStars(){
        MapItems[] stars = new MapItems[amount];
        for(int i = 0; i < amount; i++ ){
            if(i<amount*0.4) {
                stars[i] = starTwinkle;
            }
            else{
                stars[i] = starCircle;
            }
        }
        return stars;
    }
*/
}
